# import the math modul so i can use math.pi and math.sqrt
import "math"

# print a desription of this program for the user to see
print("This program computes and outpus the volume of a tire")

# Get the width, ratio of the tire and diameter from the user.
width = float(input("Enter the width of the tire in mm (ex 205)"))
aspectratio = float(input("Enter the aspect ratio of the tire in mm(ex 60)"))
diameter = float(input("Enter the diameter of the wheel in inches(ex 15)"))

# Compute the volum of a tire.
width = 